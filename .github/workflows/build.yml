name: Build Clash Config
# 触发规则
on:
  push:
    branches: [ main ]
  # 新增定时任务
  schedule:
    # 示例1：北京时间每日早8点 → UTC 0点，表达式：0 0 * * *
    - cron: '0 8 * * *'
    # 示例2：每6小时更新一次（UTC 0/6/12/18点，对应北京时间8/14/20/次日2点）
    # - cron: '0 */12 * * *'
  # 手动触发按钮（可选，方便手动跑）
  workflow_dispatch:
